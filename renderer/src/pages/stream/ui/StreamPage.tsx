import { StreamWorkbench, type StreamWorkbenchProps } from '@/widgets/stream-layout/ui/StreamLayout';

export function StreamPage(props: StreamWorkbenchProps) {
  return <StreamWorkbench {...props} />;
}

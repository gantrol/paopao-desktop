import { SortingWorkbench, type SortingPaneProps } from '@/widgets/sorting-workspace/ui/SortingWorkspace';

export function SortingPage(props: SortingPaneProps) {
  return <SortingWorkbench {...props} />;
}
